#include "features.h"
#include <thread>

void features::visuals::glow() noexcept
{
	while (vars::run) {
		std::this_thread::sleep_for(std::chrono::milliseconds(2));

		// FEATURES HERE
	}
}

void features::visuals::fovchanger() noexcept
{
	while (vars::run) {
		std::this_thread::sleep_for(std::chrono::milliseconds(2));

		// FEATURES HERE
	}
}

void features::visuals::noflash() noexcept
{
	while (vars::run) {
		std::this_thread::sleep_for(std::chrono::milliseconds(2));

		// FEATURES HERE
	}
}