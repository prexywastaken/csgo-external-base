#include "features.h"
#include <thread>

void features::legitbot::aimbot() noexcept 
{
	while (vars::run) {
		std::this_thread::sleep_for(std::chrono::milliseconds(2));

		// FEATURES HERE
	}
}

void features::legitbot::triggerbot() noexcept
{
	while (vars::run) {
		std::this_thread::sleep_for(std::chrono::milliseconds(2));

		// FEATURES HERE
	}
}