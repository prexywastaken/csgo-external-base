#include "features.h"
#include <thread>

void features::misc::bhop() noexcept
{
	while (vars::run) {
		std::this_thread::sleep_for(std::chrono::milliseconds(2));

		// FEATURES HERE
	}
}

void features::misc::autostrafe() noexcept
{
	while (vars::run) {
		std::this_thread::sleep_for(std::chrono::milliseconds(2));

		// FEATURES HERE
	}
}

void features::misc::fakelag() noexcept
{
	while (vars::run) {
		std::this_thread::sleep_for(std::chrono::milliseconds(2));

		// FEATURES HERE
	}
}